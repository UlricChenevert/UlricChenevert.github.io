// Pull from either the global character data or the configured data and provide a configured value
// Pick from a finite attitude list
// Regenerate names differently from the name generator provided
// Randomize attitude selection
// export class Entanglements {
//     constructor(public Identifier : PronounType, public Attitudes : DispositionType, public Type : NameType) {}
// }
export class EntanglementPartPicker {
    Randomize;
    GlobalCharacterData;
    FriendlyName;
    ViewUrl;
    isLoading;
    HTMLandKnockoutRequestCallback;
    Init;
    Destruction;
    Evaluate;
}
